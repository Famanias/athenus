# Sample Media Directory

This directory (`data/samples/`) contains small, public-domain sample video and audio assets used by unit and integration tests.

Unlike runtime uploads (`data/uploads/`), databases (`data/athenus.db`), or vector indices (`data/qdrant/`), files in `data/samples/` are tracked in Git to allow new developers to onboard and run automated test suites out of the box.
